services:
  - type: static
    name: newsapp
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    # Root directory should be empty (root of repo) - this is the default
    # Only specify rootDir if your project is in a subdirectory
    
    # Environment variables (add your API keys here if needed)
    # Uncomment and add your environment variables:
    # envVars:
    #   - key: VITE_BASE_PATH
    #     value: /
    #   - key: VITE_API_KEY
    #     value: your-api-key-here

